{
  "name": "O+OC_checker_v5",
  "is_template": true,
  "args": {
    "gerund": "$1"
  },
  "API": "[repo] verify the phrase F -> O+OC",
  "callback": [
    "[repo] the phrase F -> O+OC",
    "[repo] O of the phrase F -> C",
    "[repo] OC of the phrase F -> $1 B"
  ],
  "data": [
    {
      "description": "main",
      "commands": [
        "[input] [repo] verify the phrase C $1 B -> O+OC",
        "[input] [repo] type of the phrase $1 B -> gerund",
        "[input] [repo] grammar usage of C -> an object",
        "[output] [repo] the phrase C $1 B -> O+OC",
        "[output] [repo] O of the phrase C $1 B -> C",
        "[output] [repo] OC of the phrase C $1 B -> $1 B"
      ]
    },
    {
      "description": "preparatory checks:1",
      "commands": [
        "[input] [repo] verify the phrase C $1 B -> O+OC",
        "[output] [repo] verify type of the phrase $1 B -> gerund"
      ]
    },
    {
      "description": "preparatory checks:2",
      "commands": [
        "[input] [repo] verify the phrase C $1 B -> O+OC",
        "[input] [repo] type of the phrase $1 B -> gerund",
        "[output] [repo] verify grammar usage of C -> an object"
      ]
    }
  ]
}