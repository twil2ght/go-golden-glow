## description of special parameters

| key    | value           | description                                                                                       |
|--------|-----------------|---------------------------------------------------------------------------------------------------|
| static | boolean         | if the data is static,then it has no extra `gets`                                                 |
| type   | `merge`/`route` | - `merge`:merge two or more data into a single one<br/>- `route`:build a route to call other data |
