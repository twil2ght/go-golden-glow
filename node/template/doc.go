// Package template provides a set of templates for generating code.
// the template "$1 is $2" is the only template that is prohibited
// (can be a template when it is the only one)
package template
